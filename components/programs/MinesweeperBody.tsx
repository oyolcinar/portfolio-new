'use client';

import type { ProgramBodyProps } from '@/types';

export default function MinesweeperBody({ windowId }: ProgramBodyProps) {
  // TODO: Implement minesweeper game
  return (
    <div className="w-full h-full bg-[#c0c0c0] flex items-center justify-center text-[0.55rem] text-[#a0a0a0]">
      Minesweeper — Coming Soon
    </div>
  );
}
